#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
int main(int argc, char const *argv[])
{ 
    int t, *A, i, j, tmp;

    scanf("%d", &t);

    for ( i = 0; i < t; i++)
    {
        scanf("%d", A + i);
    }

    j = t - 1;

    for(i = 0; i < t/2; i++)
    {
        tmp = A[i];
        A[i] = A[j];
        A[j] = tmp;
        j -= 1;
    }

    for(i = 0; i < t; i++)
    {
        printf("%d ", *(A + i));
    }
    
    return 0;
}*/
